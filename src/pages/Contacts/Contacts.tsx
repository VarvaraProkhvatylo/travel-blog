import { Container } from '@mui/material'

type Props = {}

const Contacts = ({}: Props) => {
    return (
        <>
            <Container component={'div'}>Contacts</Container>
        </>
    )
}
export default Contacts
